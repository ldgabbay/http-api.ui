(function() {
    'use strict';

    angular
        .module('api')
        .controller('errorController', errorController);

    errorController.$inject = [];

    function errorController() {
    }
})();
